# Moomoo 開発用依存関係
# pip install -r requirements-dev.txt

# テスト
pytest>=9.0.0
pytest-mock>=3.14.0

# コード品質
# Ruff 0.16 expanded the default rule set; keep the established lint contract
# until the repository-wide migration is handled in a dedicated change.
ruff>=0.11.0,<0.16.0
# Keep CI aligned with the version used to establish the current type baseline.
# Upgrade separately after resolving any newly reported diagnostics.
pyright==1.1.400

# 型チェック補完
types-pyyaml>=6.0.0
types-requests>=2.32.0
