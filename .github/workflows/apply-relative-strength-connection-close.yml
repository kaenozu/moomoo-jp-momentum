name: apply relative-strength connection close

on:
  push:
    branches:
      - fix/strategy-runner-benchmark-date
    paths:
      - relative-strength-connection.trigger

permissions:
  contents: write

jobs:
  apply:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: fix/strategy-runner-benchmark-date
          fetch-depth: 0

      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Apply connection fix
        run: |
          python scripts/apply_relative_strength_connection_close.py
          rm -f scripts/apply_relative_strength_connection_close.py
          rm -f relative-strength-connection.trigger
          rm -f .github/workflows/apply-relative-strength-connection-close.yml

      - name: Commit connection fix
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -A
          git commit -m "fix: close relative-strength database connections"
          git push origin HEAD:fix/strategy-runner-benchmark-date
