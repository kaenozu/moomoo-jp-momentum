[pytest]
testpaths = tests
python_files = test_*.py
norecursedirs = scripts data reports logs .github
markers =
    slow: intentionally slow tests (e.g. network/retry simulation)
    integration: tests that interact with real databases or files
    network: tests that make external network calls
    opend: tests that require a running moomoo OpenD instance
